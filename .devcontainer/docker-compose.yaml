version: '3'
services:
  alphalink:
    build:
      dockerfile: Dockerfile
      context: ..
    volumes:
   # Update this to wherever you want VS Code to mount the folder of your project
      - ..:/workspaces:cached
